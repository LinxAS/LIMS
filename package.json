{
  "name": "lims-mockup",
  "version": "1.0.0",
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch",
    "build": "cds build"
  },
  "dependencies": {
    "@sap/cds": "^8",
    "express": "^4",
    "@sap/cds-dk": "^8",
    "@cap-js/sqlite": "^1"
  },
  "devDependencies": {
    "@sap/ux-ui5-tooling": "^1"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": { "database": "db/lims.db" }
      }
    }
  }
}
